# Used by `npm run dev` only. The local API (iigl-node, PORT 3000), so a
# change to an endpoint shows up here before it is deployed. Production builds
# read .env.production instead.
VITE_API_URL=http://localhost:3000/api
